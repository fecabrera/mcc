# Examples

A tour of the language, grouped into topical folders and ordered roughly by
complexity within each. Every example is runnable:

```bash
pipenv run python -m mcc examples/<folder>/<name>.mc --run
```

Start with [basics/helloworld.mc](basics/helloworld.mc) and read down; each
folder assumes the concepts from the ones above it.

## basics/

The smallest programs and the scalar core: printing, bindings, constants,
literals, and integer/float arithmetic.

| Example | Shows |
|---------|-------|
| [helloworld.mc](basics/helloworld.mc) | the smallest program: `import "std"`, `fn main`, `println` (see [helloworld-libc.mc](basics/helloworld-libc.mc) for the raw-libc `printf` version) |
| [variables.mc](basics/variables.mc) | `let`, type inference, annotations, every integer width, mutation, uninitialized `let x: T;` |
| [constants.mc](basics/constants.mc) | `const` compile-time constants, constant expressions, sizing arrays, string consts |
| [literals.mc](basics/literals.mc) | hexadecimal integer literals, `char` character literals and escapes |
| [arithmetic.mc](basics/arithmetic.mc) | operators, precedence, comparisons, `!`, float math, `abs` |
| [compound_assignment.mc](basics/compound_assignment.mc) | `+= -= *= /= %= &= \|= ^= <<= >>=`, target evaluated once, through variables/pointers/elements/fields, floats |
| [unsigned.mc](basics/unsigned.mc) | unsigned division/comparison semantics, zero-extension |

## control-flow/

Branching, looping, compile-time selection, the never-executed-path
assertion (`unreachable`), the scope-based constructs (`defer`, block
expressions, iteration), and the opt-in dead-code report on statements the
diverging constructs make unreachable.

| Example | Shows |
|---------|-------|
| [branching.mc](control-flow/branching.mc) | `if` / `else if` / `else`, integer (non-zero) conditions, the short-circuiting `and` / `or` logical operators |
| [while.mc](control-flow/while.mc) | `while` loops, `break` / `continue`, nested loops |
| [until.mc](control-flow/until.mc) | `until`, the inverse of `while`: loop while the condition is false |
| [conditional.mc](control-flow/conditional.mc) | `@if` / `@else` compile-time selection over `TARGET_OS` / `TARGET_ARCH`, `@symbol` per platform |
| [case_when.mc](control-flow/case_when.mc) | `case` / `when` / `else:` with no fall-through, integer and character subjects, multi-value arms |
| [unreachable.mc](control-flow/unreachable.mc) | the `unreachable;` statement asserting a path never executes (UB if reached): `else: unreachable;` marking a `case` exhaustive, no dummy trailing return needed after it |
| [defer.mc](control-flow/defer.mc) | `defer` cleanup at scope exit (return/break included), LIFO order, the block form |
| [block_expressions.mc](control-flow/block_expressions.mc) | `{ ...; emit v; }` as a value, contained temporaries, branch emits, `defer` inside |
| [forever.mc](control-flow/forever.mc) | constant-condition loop folding: a `break`-free `while (true)` / `until (false)` / `while (CONST)` diverges like a `return`, so no dummy trailing return or emit is needed after it, and one `break` anywhere in the body restores the exit |
| [dead_code.mc](control-flow/dead_code.mc) | the opt-in `-Wdead-code` class reporting statements the generator silently drops: one warning per dead region (at its first statement) after `return` / `break` / `continue` / `unreachable` / `emit` / a `@noreturn` call / an all-paths-diverging `if` / a `break`-free forever loop, a dead `defer` that never registers, and the live gate: code after a breaking `while (true)` never warns |
| [iteration.mc](control-flow/iteration.mc) | `for x in` over the `_it`/`_next` protocol (list, set, dict), the builtin `pair<K, V>`, `break` / `continue` |
| [enumerate.mc](control-flow/enumerate.mc) | the builtin `enumerate` position counter — the `enumerated<T> { index; value }` it yields, and value-vs-`&` borrowing |
| [ranges.mc](control-flow/ranges.mc) | the builtin `range` — `for i in range(start, end)` / `range(end)` counting loops, lowered directly with no allocation, element type inferred from the bounds |

## functions/

Defining and calling functions: void/recursion, forward declarations, the
parameter modifiers (`const`, `&`, `@noalias`, `@nonnull`), `&` returns
(functions returning lvalues), overload sets
(concrete, generic, mixed, and open across modules), never-returning
functions (`@noreturn`) and the stdlib `panic`/`assert` pair built on them
(with the opt-in `-Wnoreturn-own` leak report),
variadics (C `...` and native collecting), and function pointers, including
the function types that carry a `@nonnull` contract or spell the `&`/`const &`
hidden-reference conventions and the `-> &` return convention.

| Example | Shows |
|---------|-------|
| [functions.mc](functions/functions.mc) | void functions, any-order definitions, recursion, mutual recursion |
| [forward_declarations.mc](functions/forward_declarations.mc) | bodyless `fn` prototypes as forward declarations: a header-style prototype block with the definitions below, identical prototypes collapsing onto one declaration, the strict signature-match error; never required for ordering, but lets an imported `.mci` stub coexist with the `.mc` source |
| [const_params.mc](functions/const_params.mc) | `const` read-only parameters, the Phase B split: a by-value `const T` copy (of every type) vs the `const &T` hidden-reference view (struct and scalar alike, no copy), `const &` on a scalar, `const` on a pointer freezing the pointer not the pointee, and the owning-type hazard pointing to [destructors.mc](types/destructors.mc) |
| [reference_params.mc](functions/reference_params.mc) | `&T` write-through reference parameters (`out: &int32`): out-params with no pointer in the signature, re-lending, struct field projection, a generic `swap<T>`; `mut` is now an ordinary identifier (no longer a keyword) |
| [reference_returns.mc](functions/reference_returns.mc) | `-> &T` functions returning lvalues: an `_at`-style accessor whose call is assignable, compound-assignable (addressed once), projectable, re-lendable as a reference argument, and auto-loading in value context, plus the formation rule rooting the reference in a reference/pointer parameter or a global |
| [reference_overloads.mc](functions/reference_overloads.mc) | generic overloads mixing `&` and non-`&` positions: a reference overload next to a pointer one, rvalues dropping reference candidates, writability judged against the chosen overload, single argument evaluation |
| [overloading.mc](functions/overloading.mc) | concrete function overloading: a constructor-flavored `counter_init` family dispatched by arity and by argument type, a string literal still adapting to a `slice<const char>` member, the must-differ-in-parameter-types rule |
| [mixed_overloads.mc](functions/mixed_overloads.mc) | a generic template and concrete functions sharing one name: the (is-concrete, specificity) rank, concrete fast paths winning exact matches, the generic catch-all covering the rest, explicit type args selecting among the generic candidates only |
| [overload_subsumption.mc](functions/overload_subsumption.mc) | subsumption ordering of rank-tied templates: the diagonal `same<T>(x: T, y: T)` beating the open `same<T, U>` for agreeing arguments (a three-way chain resolving to the full diagonal), the integer-only literal viability rule dropping a diagonal on a `point<float64>` so the converting sibling wins and casts, a tighter-group diagonal winning by constraint subset, and the ties that stand (mutually non-subsuming forks, rank-tied partial specializations) |
| [open_overloads.mc](functions/open_overloads.mc) | open overload sets: a module making its own struct appendable by declaring one `string::append` member into the stdlib's foreign concrete set, the whole-program union at import merge, members from two modules resolving side by side, the join that used to be a duplicate-definition error |
| [override.mc](functions/override.mc) | `@override`: replacing a same-pattern member of another module's set, not just extending it — overriding std/format's concrete `bool` formatter and its unbounded `<typename>` fallback so `println`'s own dispatch picks up the new bodies, the untouched higher-ranked int formatter, the cross-module / source-visible / one-target rules |
| [pointer_decay.mc](functions/pointer_decay.mc) | a proven-non-null `T*` decaying into a `const &` or `&` hidden-reference slot: one call shape for a stack value and a null-guarded heap pointer, rvalue `&x` decaying too, the narrowed fact surviving the call |
| [noalias.mc](functions/noalias.mc) | `@noalias` pointer parameters (C's `restrict`): the unchecked no-overlap promise that lets the optimizer treat a copy's regions as disjoint |
| [nonnull.mc](functions/nonnull.mc) | `@nonnull` pointer parameters: the checked "definitely non-null" refinement — call sites must prove the argument non-null, the callee skips the re-check |
| [nonnull_narrowing.mc](functions/nonnull_narrowing.mc) | flow-narrowing for @nonnull: the three null-check guard shapes (the `if (p != null)` then branch, the diverging early `if (p == null)` guard, `and`/`or` chains threading both) that prove a plain `T*` local with no `p!`, purely at compile time |
| [nonnull_loops.mc](functions/nonnull_loops.mc) | narrowed facts crossing loops: a loop kills only the facts it could invalidate (so guard-then-loop just works, in the body and past the exit), `while (p != null)` proves p on every iteration, and a `while (p == null)` retry loop proves p after it |
| [nonnull_projections.mc](functions/nonnull_projections.mc) | flow-narrowing for field projections: the same guards prove a pointer-typed struct field (`b->data`, keyed by access path at any depth) into @nonnull slots, the call write-effect refinement (a call kills the fact unless the callee is proven transitively write-free; stores and loop entry always kill), and `let q = b->data;` binding a checked field to a name fact that survives writing calls and loops |
| [nonnull_assert.mc](functions/nonnull_assert.mc) | the postfix `p!` non-null assertion, @nonnull's escape hatch where narrowing cannot see the invariant: a zero-cost static proof for heap/returned pointers (null is then UB), one hatch at a `let` seeding all later uses, and the `!=` lexing gotcha |
| [noreturn.mc](functions/noreturn.mc) | `@noreturn` void functions that never return: a panic-style exit helper, calls diverging like a `return` (no dummy return after), the C-idiomatic `if (p == null) abort();` guard narrowing into @nonnull, the legal `while (true)` spin body, defers skipped at the call |
| [panic_assert.mc](functions/panic_assert.mc) | the stdlib `panic`/`assert` pair from std/io, one verbatim member each: `panic(msg)` writing its message verbatim to stderr then aborting (SIGABRT, exit 134, defers skipped, stdout flushed; no `panic(fmt, args...)` collector exists), the message a single `T extends slice<const char>` binding literals and owned strings alike, the blessed plain-message style vs formatting explicitly at the call (`panic(f"...")` renders a string value whose statement-end drop never runs -- the leak on the dying path, made visible by a drop-stamped probe that closes after a passing `assert`'s rendered message and never closes in the aborting `panic`'s), doubling as the living demo of the opt-in `-Wnoreturn-own` class -- the panic finale is its live trigger, reporting exactly that guaranteed leak of an own value passed to a `@noreturn` function (`assert` never warns, it returns; the let-first spelling silences the site; `-Wall -Werror` promotes it `[-Werror=noreturn-own]`, so CI compiles this file at plain `-Werror` like the other class demos) -- the panic guard narrowing a null check where `assert(p != null, ...)` does not, and a deliberately-aborting finale (safe: CI compiles examples, never runs them) |
| [variadic.mc](functions/variadic.mc) | variadic `...` definitions, `va_list`, `va_start`/`va_end`, forwarding to `vsnprintf` |
| [native_variadics.mc](functions/native_variadics.mc) | native variadic collection: `args...` as sugar for a trailing `const args: slice<const any>`, extras boxed caller-side into a read-only slice walked with `for` + `case type`, zero extras giving an empty slice, the explicit spelling collecting the same, collectors in overload sets (exact arity beats collecting, more fixed parameters wins) and a generic collector binding `T` from the fixed arguments only |
| [function_pointers.mc](functions/function_pointers.mc) | `fn(...) -> R` types (incl. variadic `fn(A, ...)`), callbacks in structs, dispatch tables, `const`/`@static` function aliases, `null` callbacks |
| [nonnull_callbacks.mc](functions/nonnull_callbacks.mc) | `fn(@nonnull T*) -> R` function types carrying the @nonnull contract: a @nonnull function as a legal value (`let f = first;` infers the annotated type), calls through the value running the same call-site null proof as a direct call, the contravariant lift (a plain function into an annotated let / argument / @static dispatch table; the dropping direction a compile error), the contract-stripping `as` hatch (UB if actually null), and the extern asymmetry (`let n = strlen;` checks strictly where direct extern calls stay graded) |
| [reference_callbacks.mc](functions/reference_callbacks.mc) | `fn(&T)` function types spelling the write-through reference convention: a mutating callback in a @static dispatch table, calls through the value enforcing the same writable-lvalue and pointer-decay rules as direct calls, the convention convertible in neither direction with no `as` hatch (same-shape reinterprets stay open), and `const` erasing from a function type (`fn(const T)` IS `fn(T)`, scalar and struct alike since Phase B) so `type cmp<T> = fn(const T, const T) -> bool` is inhabited at every `T` by a plain by-value comparator |
| [reference_return_callbacks.mc](functions/reference_return_callbacks.mc) | `fn(...) -> &T` function types carrying the return convention: a reference-returning accessor as a legal value (inferred and declared), calls through the value assignable and compound-assignable like a direct call, a field-held callee written through, the result re-lent into an `&` parameter, an indirect callee vouching inside another function's formation chain, and the convention convertible in neither direction with no `as` hatch |

## types/

The type system: aliases, arrays and strings, enums, the `error`/`result`
pair of recoverable error handling, structs (and their literals, qualified
methods with their dot-call sugar, constructors and their automatic
destructors, `extends`
bases with the method families they hand down and dispatch dynamically
through base views, flexible array members),
unions, tuples, the `any`
box, the `typename` builtin, and generics, plus the compile-time
directives that check a build's invariants, its configuration, and its use of
deprecated or removed functions, and the opt-in warning classes that report
on legal-but-unproven code.

| Example | Shows |
|---------|-------|
| [type_aliases.mc](types/type_aliases.mc) | `type <name> = <type>;` transparent aliases for builtins, pointers, function pointers, and structs; `type` as an identifier |
| [generic_alias.mc](types/generic_alias.mc) | `type entry<T> = pair<char*, T>;` generic aliases: a type-parameter list naming a family of types, staying transparent (`entry<int32>` *is* `pair<char*, int32>`, one shared instantiation), a `cmp<T>` comparator shape, an inert unused parameter (unlike a struct's), and a defaulted alias parameter |
| [arrays.mc](types/arrays.mc) | fixed-size `T[N]` arrays (`N` a constant expression), indexing, `sizeof`, pointer decay, multi-dim, a `@static` buffer |
| [strings.mc](types/strings.mc) | string literals as `char[N]` text arrays (NUL counted): owned vs `char*`, inferred/oversize sizes, decay, mutation, `len`, indexing, borrowing as `slice<char>`; contrast with a raw `uint8[N]` byte buffer |
| [string_tables.mc](types/string_tables.mc) | string-literal elements adapting to `slice<char>` / `slice<const char>` with no per-element `as`: a local lookup table, a `@static` table and scalar as constant `{pointer, length}` views, NUL-free lengths, runtime indexing |
| [enums.mc](types/enums.mc) | `enum Name: T { M = v, ... }`, `Enum::Member`, the enum name as a type, custom underlying types (uint64 flags, string members), members referencing earlier ones |
| [derived_enums.mc](types/derived_enums.mc) | `enum b: a` member reuse: the derived enum copies the base's members and adopts its underlying type (pointers too), inherited members fold equal and are referenceable by new ones, transitive chains; compile-time only, no new type checking |
| [error_handling.mc](types/error_handling.mc) | recoverable errors as values: the nominal `error` declaration (auto-numbering from 1 with explicit values continuing the count, display strings as data, truthiness against the reserved zero state, `==`/`!=`, `case`, the `as int32` read-out and nothing else), the builtin `result<T, E>` / `result<E>` with the explicit, context-typed `ok()`/`error()` constructors, and every consuming form: the `let ret, err = f();` destructure (falsy no-error state on ok, zero value of `T` on error), `try f() except (err) { ... }` with an `emit` fallback or a diverging handler, `else` as the ok arm (skipped on the fallback path), the unary-level `try` composing as an operand, the obligation-free statement-position handler over `result<E>`, bare `try f()` propagation (enclosing return must carry the same error type, statement-position `try f();` over `result<E>`, never implicitly re-wrapped), the `??` fallback (lazy, a full-expression or emit-block right-hand side, binding looser than every binary operator so it extends greedily to the end of the expression), and the block-scoped `try (r = f()) { ... } except (err) { ... }` statement with no else arm, plus rendering an error to text with `error_name`/`error_message` (identifier vs display string, falling back to the identifier, the reserved zero state rendering empty) and the `let _ = f();` discard that silences `-Wunused-result` |
| [structs.mc](types/structs.mc) | structs, generic structs, `->` / `.`, `null`, struct literals, a hand-built linked list |
| [struct_literals.mc](types/struct_literals.mc) | `Name { field = value, ... }` literals (the `struct` keyword optional): omitted fields zeroed or set to a `= default`, free field order, generics (inferred type args), nesting, as args/returns/through a pointer, a string/array literal borrowing into a slice-typed field (never driving generic inference), and the bare, type-inferred `{ field = value, ... }` form that drops the name where context fixes the struct type (let/assignment/return/argument/element/nested field, overloads resolved by field names) |
| [methods.mc](types/methods.mc) | qualified methods `fn Type::name(...)` called by explicit qualified name `Type::name(args)`: the receiver an ordinary parameter (`Type::` is purely a namespace -- no enforced `self` convention, the only rule being the qualifier is a declared struct), `reference self` mutation visible to the caller, the same method name on two structs not colliding, an overloaded `Type::name` dispatching by argument; the explicit form every `.method()` dot call ([method_calls.mc](types/method_calls.mc)) desugars into |
| [generic_methods.mc](types/generic_methods.mc) | methods on a GENERIC struct, the struct's type parameters written before the `::` (`fn point<T>::name`): the explicit `self: point<T>` receiver naming its type argument, `T` inferred from the call or PINNED by a qualifier spelling the instantiation (`point<float64>::sum(pf)`, the receiver coercion-checked against the pin), a no-receiver member (`point<float64>::origin()`) callable only through the pin, monomorphization to one function per element type across `point<int32>` / `point<float64>`, `const self` / `reference self`, and a method with its OWN type parameter (`box<T>::labeled<U>`) merging into one template with `T` and `U` both inferred (the method's own list stays inference-only at a call; a second written list is a parse error), plus the no-shadow rule |
| [method_specialization.mc](types/method_specialization.mc) | a concrete body for ONE instantiation of a generic struct, `fn Type<Concrete>::method(...)`, coexisting with the generic `fn Type<T>::method(...)` and OUTRANKING it for a matching receiver (registered as an ordinary concrete overload, dispatched by the existing concrete-beats-generic ranking): a `point<float64>::magnitude` specialization skipping the `as float64` casts the generic needs, a user-struct key `holder<widget>::describe`, runtime markers proving which body ran; all-concrete pre-`::` args here (mixing in fresh type parameters is a partial specialization, next file), and a lone specialization needs no generic base |
| [method_partial_specialization.mc](types/method_partial_specialization.mc) | PARTIAL specialization, mixing concrete types and fresh type parameters before the `::` (`fn pair<int32, U>::describe`): full > partial > generic dispatch through the existing overload ranking (full is a concrete overload, a partial's concrete positions win on pattern specificity within the open tier), a mismatched receiver falling through to the generic, fresh names as real inferred type parameters prepending the method's own list (`pair<int32, U>::tag<W>`), and a bounded fresh position (`U: int8 \| int16`) whose group filters which receivers the partial admits |
| [method_alias.mc](types/method_alias.mc) | methods through `type` aliases and on BUILTIN types: the qualifier is chased to the canonical name, so alias and canonical spellings declare and call ONE family; a plain alias qualifier (`fn pointf::magnitude` with `type pointf = point<float64>`) IS the specialization, a generic alias with written arguments substitutes through its target (`fn swap<int32, U>::pick` with `type swap<X, Y> = pair<Y, X>` IS the partial `fn pair<U, int32>::pick`), a duplicate-position alias (`type diag<T> = pair<T, T>`) is a diagonal constraint matching only `pair<X, X>` (falling through to a generic sibling on mismatch, winning the [subsumption](functions/overload_subsumption.mc) tie-break against it on an agreeing receiver), a bare generic-alias qualifier cannot declare (annotate it, `fn pt<T>::mk`; a fully-defaulted alias is the one bare exception), CALL qualifiers following the completeness line -- a bare COMPLETE alias injects the instantiation it names and pins the receiver (`pointf::magnitude(pf)` IS `point<float64>::magnitude(pf)`), a bare incomplete one chases by name and infers, and written type arguments substitute through at the call too, permutation included (`swap<int32, float64>::pick(sw)` pinning `pair<float64, int32>`) -- alias spellings in signatures stay transparent to inference, and builtins are qualifiers too (`fn int32::clamp`, `fn slice<T>::first`, callable through an alias as one family, pinnable at a call like `slice<int32>::first(s)`, never specializable in a declaration) |
| [method_calls.mc](types/method_calls.mc) | the `.method()` call sugar: `recv.method(args)` desugaring to `Type::method(recv, args)` with the receiver passed verbatim (overloads, `&`-receiver legality, and diagnostics the desugared call's own; the qualified spelling staying valid); fields shadowing methods (a fn-typed field keeps the field call, the method reachable qualified, only neither-field-nor-method getting the combined error, no bound-method values), a pointer receiver auto-dereffing exactly one hop (`->` stays fields-only, `S**` an error), rvalue receivers evaluating once into a hidden const local (chaining works, reference-self on a temporary rejected, a reference-RETURNING receiver re-lending its lvalue), reference-returning dot calls as lvalues (`l.at(0) = 9`, `+=`, chained store targets, formation chaining), generic receivers inferring `T` (type arguments never spelled at a dot call), builtin receivers (`'q'.upper()` via std/char, a declared `slice<T>::first`, and a bare string literal `"hello".first()` adapting to `slice<const char>` so the slice family reaches it -- a literal-only fallback that never shadows the `char*` decay a C call needs), and dot calls in f-string holes |
| [properties.mc](types/properties.mc) | `@property`: a method reachable through field syntax -- `s.length` calls `Type::length(s)` (a zero-argument, receiver-only accessor drops its parentheses), the call spelling `s.length()` staying valid beside it; a read-only `const self` getter, a settable `-> &` property whose lvalue takes `=`/`+=` (through the reference return), an explicit `@property("get")`/`@property("set")` pair for write-path logic (a clamping setter, `g.level = v` calling `gauge::level(g, v)`, `op=` as read-modify-write through both, a setter's return discarded, the pair never mixing with the bare form on one family), inheritance through `extends`, generic properties binding `T` from the receiver, pointer receivers auto-dereffing one hop, a real field shadowing a property (field-first, the method then reachable only qualified), and the stdlib's `stack<T>::length`; the declaration-shape errors (only a method, only its receiver -- plus the assigned value for a setter, must return a value, needs a body) |
| [accessors.mc](types/accessors.mc) | `@accessor`: the method behind a type's `[]` operator -- `xs[i]` calls `Type::at(xs, i)` (the call spellings staying valid beside it), with the indices as ordinary arguments (any number, of any type: a two-index `g[r, c]` grid, a string-keyed `d["key"]` dict) and native `[]` (pointer/array/slice/tuple) never consulting an accessor; the bare `-> &` form whose lvalue takes `=`/`+=` through the reference return, an explicit `@accessor("get")`/`@accessor("set")` pair for write-path logic (a clamping setter, indices first and the assigned value last, `op=` as read-modify-write through both, never mixing with the bare form), one family name per type (`[]` carries none to pick by), inheritance through `extends`, and the stdlib adopters: `list<T>::at` bare (string inherits it) and `dict<V>::at` as the pair (`d[k]` unchecked -- guard with `.has` -- and `d[k] = v` inserting through `.set`) |
| [constructors.mc](types/constructors.mc) | the constructor call sugar: a method named `constructor` makes its type callable, `S(args)` desugaring to exactly `let s: S; S::constructor(s, args);` (the slot default-initialized first, `let` binding it directly with no copy, diagnostics the family call's own); every head form -- a bare non-generic struct, explicit type arguments `point<float64>(1, 1)` pinning `T` so a converting `constructor<U>` beats the int-literal-rejecting diagonal AND CHAINS to it -- `point<T>::constructor(self, x as T, y as T)` inside the generic body, the enclosing `T` resolved through the live instantiation (explicit type arguments at a `::` call) -- a BARE generic head `point(1.5, 2.5)` inferring the instantiation from the arguments (int literals leaning int32), a transparent alias `pointf(3, 4)`, a fully-defaulted generic constructing at its defaults, and a builtin (`char(65)`) once someone declares its family; expression/return positions, the desugared spelling staying first-class (bare or pinned, `point<int32>::constructor(d, 5, 6)`), no-constructor calls WITH arguments erroring (never a cast) while struct literals remain the no-constructor spelling, and the implicit empty constructor: `T()` with zero arguments as exactly `let t: T;` (field defaults in, a declared arg-taking family not suppressing it, a receiver-only member claiming it as the declared non-fallback, builtin heads included) |
| [destructors.mc](types/destructors.mc) | automatic cleanup of stack-constructed values: a method named `destructor` makes the constructor-sugar `let` (and ONLY it: `let t = T(args);` / `let t = T();`) schedule `defer T::destructor(t);` on the enclosing block's defer stack, shared verbatim -- the RAII shape (open in the constructor, close in the destructor, an early return proving every path cleans up), LIFO with explicit defers in reverse construction order, per-iteration loop cleanup, the destructor seeing the value's latest state, manual base chaining (`file::destructor(self);`) beside a derived type inheriting the base's destructor whole, a generic owner destroying its member FIELD by chaining at the enclosing `T` (`handle<T>::destructor(self.h)`, the pinned spelling that makes the chain expressible), and the opt-out spellings that schedule nothing (manual construction owning its own destroy, struct-literal lets, copies -- a copy of a value whose type declares a destructor now firing the shipped `-Wdestructor-copy`, exempted by `move(v)` or a `const &` borrow, with this file the CI demo compiled at plain `-Werror`); the whole-value return/emit hard error with its hatches, the const view still destroyed, and the stack-lets-only scope in the tail comments |
| [own_returns.mc](types/own_returns.mc) | move-out returns, `-> own T`: the whole-value return error lifted for a function that declares the transfer in its signature -- returning the auto-destructed local cancels its scheduled destructor on that path only (an early return still destroys it) and the caller's let ADOPTS the obligation like a constructor-sugar let; the strict formation rule (unmarked returns: the constructed local, a fresh constructor expression, or a chained own call) with the builtin-shaped `move(v)` (claimed by call shape like `ok()`/`error()`; `return move(v);` or `return ok(move(v));`) as the explicit assertion for a plain copy (the pop-owned idiom, a `slot::take` moving a field out), result composition through the ok payload (`return ok(local)` transfers, `return error(...)` destroys normally, the except-let adopting and its emitted fallback riding the same schedule), the inert consumptions that drop the obligation (discard, argument, `?? fallback`), the no-op on destructor-less types that keeps generic `-> own T` writable, and the marker riding function-pointer types (`fn(int32) -> own conn`: a factory value's call still adopts; implicit retyping across the contract rejects both ways, `as` being the hatch); a print-stamped resource shows one open and one close per value on every path |
| [own_receivers.mc](types/own_receivers.mc) | the consuming receiver `own self: T` and the `own` by-value owning parameter marker (`fn drain(own b: box)`): a method (or free function) that takes ownership BY MOVE and drops the value at the end of its body -- the receiver-side mirror of `-> own`, monomorphic and not dispatch-eligible -- with the same relinquish discipline as [own_returns.mc](types/own_returns.mc) (a fresh temporary or chained dot-call receiver ADOPTED with no `move`, a named owned local relinquished with `move(a)`, its scheduled destructor cancelled and later use a use-after-move error), a consuming builder step handing the value back with `return self` so a chain over fresh temporaries drops exactly once at the terminal step, and the still-not-supported forms (`own self: &T`, `own` on `@extern`) quoted in the tail (the generic/overloaded/function-value forms now ship -- see [own_generic.mc](types/own_generic.mc)) |
| [own_generic.mc](types/own_generic.mc) | `own` on every non-direct call path (the forms [own_receivers.mc](types/own_receivers.mc) deferred): a CONSUMING METHOD on a generic struct (`fn pair<T>::into_sum(own self: pair<T>) -> T` -- the container idiom `list<T>::into_iter`/`string::into_bytes` follow, the receiver dropping inside the method before the result flows out), an OVERLOADED consuming set (members must AGREE on which positions are `own`; a set mixing a consuming and a copying member at one name is rejected), and an `own` FUNCTION VALUE (`let f = drain` typed `fn(own pair<int32>) -> int32`, a DISTINCT type from `fn(pair<int32>) -> int32` -- one transfers ownership, the other copies, neither converting to the other -- a call through it enforcing the move like a direct call); the identical relinquish discipline throughout (a fresh value adopted, a named local `move`d, the qualified `Type::m(move(p))` form on a named local since a dot receiver has no place to write move), with the three remaining errors (owned-reference receiver, a disagreeing overload set, an own/plain function-value mismatch) quoted in the tail |
| [own_drops.mc](types/own_drops.mc) | statement-end destruction of expression-position `-> own` temporaries: every receiverless consumption of an own call ([own_returns.mc](types/own_returns.mc)) DROPS the handed-over temporary once the full call chain -- the statement -- ends, with open/close stamps pinning exactly where each drop lands -- discarding the call (`make(1);` and the `try load(k);` unwrap), argument position (alive through the callee, closing after it returns, several temps in one statement closing newest-first), the full-chain rule (`outer(use(make(5)))` closes only after the OUTER call returns), method chaining, and the `try ... ?? fallback` mix (the ok arm drops the unwrapped payload with the fallback never built, the error arm drops the built fallback; under a let it now ADOPTS on both arms, closing its old never-adopts gap) -- beside the unchanged adopting lets (plain and `let _`, LIFO at scope end), transfer (`return make(k);` forwards without a drop), return position (the temp closes inside the callee, after the return value is computed), conditional arms dropping only what executed, and statement temps dying before the scope's defers; the assignment sharp edge documented plainly (the temp drops after the statement, the variable's bitwise copy names the destroyed resource and its adopted schedule closes it AGAIN at scope end while the overwritten value never closes; `-Wdestructor-copy` (now shipped, demoed in [destructors.mc](types/destructors.mc)) flags the copy), and the two no-ops (a destructor-less `own` has nothing to drop; a mixed own/plain overload set neither adopts nor drops, the one remaining documented leak) |
| [fstring_values.mc](types/fstring_values.mc) | string-valued f-strings: outside an `@format` call's format string an f-string desugars to a synthesized `slice::format` call, an `-> own string` value (`f"x = {x}"` IS `"x = {}".format(x)`) -- a `let` adopts the rendering (typed `let t: string` too, destroyed at scope end), an argument's temporary drops at statement end after the callee returns (own HOLE temporaries included, drop-stamped, closing [own_drops.mc](types/own_drops.mc)'s recorded follow-up), methods chain off the rvalue (`f"{x}".equals("255")`), `return f"..."` transfers out of an `-> own string` function, and a zero-hole `f"hi"` still renders, its brace escapes collapsing where a plain literal now goes out verbatim; the compile-time splice still wins at surviving `@format` slots (`.format` literals and user collectors) with a collected f-string past a plain format string rendering as an ordinary value, and the explicit `f"..." as slice<const char>` borrow is the hatch into concrete slice positions (no implicit string-to-slice coercion; the honest mismatch, the constant/addressing rejections, and the missing-`std/slice` error quoted in the comments) |
| [extends.mc](types/extends.mc) | named-base `struct point3 extends point`: base fields laid out first as a true prefix, inherited fields named directly (in literals too), the explicit pointer/value upcasts, base field defaults carrying into a derived literal, bodyless specialization as a branding type |
| [generic_extends.mc](types/generic_extends.mc) | a generic struct extending a generic base built from its own parameters (`entry<K, V> extends cell<K, V>`), monomorphized together per instantiation; the per-instantiation upcast, and the literal caveat: inference sees only the extender's own fields, so base fields need explicit type args |
| [method_inheritance.mc](types/method_inheritance.mc) | method inheritance through `extends`: a derived struct's family calls resolving over the MERGED set of its own members plus every base hop's rebased at the declared instantiation (constructors included, so an inherited diagonal acts as a concrete member beside a derived converting `<U>`), the rank gaining a hop component (tier beats hop, hop beats specificity: same-shape derived members shadow inherited ones, nearer bases shadow farther), the receiver position (only) upcasting so a reference-self method writes the derived value's base prefix in place and a derived constructor chains via the explicit base-qualified call, inherited constructors leaving derived-added fields at their defaults, and a generic derivation staying generic with bare-head inference |
| [polymorphic_views.mc](types/polymorphic_views.mc) | dynamic dispatch through base views (the runtime sequel to method_inheritance.mc's static resolution): a method call through a base-typed reference `&A` / `const &A` routing to the runtime object's own `@override`, the table riding in the two-word fat REFERENCE (not the value, so objects keep their exact byte layout) and forming implicitly when a derived value passes into any `&A` slot, dispatch through a mid-chain view still reaching the leaf override, a never-overridden family staying a direct call, `self` carrying the table so a base method re-dispatches, copy-on-read as prefix extraction (a value copied OUT of a view carries no table, so its call binds to its static type: data slicing yes, behavioral slicing impossible), and a `-> &base` reference return carrying the view -- a forwarded reference keeps its runtime table across the hop (and across a re-return), so a method call on the returned reference still dispatches the derived override |
| [flexible_array_members.mc](types/flexible_array_members.mc) | a trailing `field: T[]` flexible array member: adds 0 to `sizeof`, decays to a `T*` at the struct's tail, one allocation for header plus elements |
| [unions.mc](types/unions.mc) | `union Name { ... }` members sharing one storage (all at offset 0): literals with one live member, cross-member byte reinterpretation (float bit patterns), generic unions |
| [static_initializers.mc](types/static_initializers.mc) | `@static` globals initialized with struct and union literals, folded to data constants: omitted/`= default` fields, nested struct/array fields, a union member narrower than the widest (zero-padded to the union's size), an empty `u{}`, struct-inside-union, and generic aggregates |
| [tuples.mc](types/tuples.mc) | `tuple<A, B, ...>`, the ad-hoc unnamed product type: paren literals `(a, b)` with per-position struct-literal coercion, multiple return values as the headline, compile-time-constant bounds-checked indexing with lvalue elements, constant slicing `t[n:m]` copying positions out into a new smaller tuple (any arity: the 1-tuple `(x,)` and the zero-sized empty tuple `()`), `len(t)` arity recovery folding into constant index/slice bounds, destructuring `let q, r = divmod(9, 4)` binding positions to names with the trailing rest binder copying the narrowed tail, the `as` cast to and from the layout-equivalent struct (struct-to-struct stays nominal) composing with destructuring, uninitialized declaration and whole-value copies, `const` params by hidden reference, generic inference through the shape, a `type` alias naming a shape, arrays of tuples, struct layout under `sizeof`, `const &` params by hidden reference, the `<tuple<...>>` println fallback |
| [any.mc](types/any.mc) | the builtin `any` tagged box (24 bytes: tag + payload): implicit boxing at assignment/argument/return, the primitive/pointer/slice boxable set (each pointer type its own tag), recovery only via `case type` with its mandatory `else`, boxing `&s` as the struct escape hatch, `@static` globals folding a constant initializer into a constant tagged box under the same tags |
| [case_type_groups.mc](types/case_type_groups.mc) | multi-type `case type` arms: comma-listed types over one binding sharing one body, the binding an implicit generic compiled once per listed type, an overload set resolving per copy (`width(n)` picks a different member per width), the duplicate-type rejection, `else` still mandatory |
| [generic_case_arms.mc](types/generic_case_arms.mc) | generic `case type` arms: an unresolved bare name introducing an arm-scoped type parameter, `when T* ptr:` catching every unclaimed pointer tag (T bound to the pointee) and `when T v:` every remaining tag, bodies monomorphized per tag from the whole program's boxed set, first-match-wins order carving concrete arms out of the fallbacks, the zero-filled `any` keeping `else` mandatory |
| [with_unwrap.mc](types/with_unwrap.mc) | the `with` statement, single-pattern sugar over a one-arm `case type`: `with (n = v as int32) body; else other;` with the binding scoped to the true branch, the `else` optional (an unmatched tag, tag 0 included, takes it, or falls through a lone `with` doing nothing), generic `T` / `T*` patterns per the case-type arm rule monomorphized per boxed tag, and the trailing-return caveat those share |
| [any_struct_boxing.mc](types/any_struct_boxing.mc) | a struct boxing into a `const any` by hidden reference: the payload a pointer to the value's storage tagged as the struct itself (`point`, not `point*`), recovered by a `when point p:` arm or `with (p = xs[0] as point)` as a read-only alias with no copy, passed on to a `const value: &point` function sharing that storage, boxed from the archetypal `slice<const any>` a variadic collects; the distinct `point` vs `point*` tags, and the still-rejected owning box / union / array |
| [typename.mc](types/typename.mc) | the `typename` builtin recovering a type's canonical name as a compile-time string: type and in-scope-variable operands (never evaluated, `sizeof`-style), generic instantiations spelled with their type args, top-level `const` stripping, folding into a `const` initializer, the static-type rule (`any` names as "any"), `typename(T)` per generic instantiation, and generic `case type` arms naming a box's dynamic type statically |
| [static_assert.mc](types/static_assert.mc) | the top-level compile-time directives `@static_assert(cond, "msg")` and `@error("msg")`: guarding a struct's layout with `sizeof`/`offsetof`/`alignof`, `const`- and `Enum::Member`-based checks, and an `@error` guarded by a dead `@if` branch to reject a target |
| [warnings.mc](types/warnings.mc) | `@warning("msg")`, `@error`'s non-fatal twin: `-D`-gated `@if` branches flagging a suspect build configuration without rejecting it, the collect-then-print warning channel, and the `-Werror` flag promoting warnings to a failed build |
| [deprecated.mc](types/deprecated.mc) | the `@deprecated("msg")` function attribute: a renamed function kept as a still-callable forwarder, every call site (and fn-as-value use) warning with the migration message, the old-API calls behind a `-D`-gated `@if` branch, `-Werror` promotion |
| [removed.mc](types/removed.mc) | the `@removed("msg")` tombstone, the lifecycle step after `@deprecated`: a bodiless declaration (the one bodiless generic allowed) turning every call into a hard compile error with the migration message, the erroring old-API call behind a `-D`-gated `@if` branch |
| [unchecked_dereference.mc](types/unchecked_dereference.mc) | the opt-in warning-class framework via its first class: `-Wunchecked-dereference` reporting possibly-null `*p` / `p->field` / `p[i]` sites in legal code, each silencer (null-check guards, proven `let`s, `@nonnull` params, postfix `!`), `-Wall`, unknown-class hard error, and the `[-Werror=<name>]` promotion |
| [generics.mc](types/generics.mc) | type inference, generic recursion, multiple type parameters |
| [generic_defaults.mc](types/generic_defaults.mc) | `<T = int64>` type-parameter defaults: the priority order (explicit > typed inference > default > untyped anchoring), a `U = T*` default referencing an earlier parameter, bare `span` / `pair<int32>` omitting a defaulted tail, struct literals filling from the default |
| [type_groups.mc](types/type_groups.mc) | closed type groups `<T: int32 \| int16 \| int8>`: the pipe-separated closed set a parameter may instantiate to as a post-deduction call-site filter, disjoint-group same-pattern templates partitioning into an overload set (the signed/unsigned formatter with no `case type`), the concrete > bounded > unbounded rank, eager checking of every member, a default naming a group member |
| [bounds.mc](types/bounds.mc) | nominal type-parameter bounds `<T extends shape>`: a parameter constrained to a struct and its declared `extends` lineage as a post-deduction filter, the layout twin rejected where the nominal rule (not a field prefix) decides, a bounded overload beside an unbounded fallback in the concrete > bounded > unbounded rank, a bound composing with a default that must satisfy it, DEPENDENT bounds whose target references type parameters (`pack<T>::matches<U extends slice<T>>` resolving per call from the qualifier's `T`, and the same-list `<S extends shape, T extends S>`), the bound joining the template symbol base (placeholder-substituted for a dependent one) and `.mci` stub |

## memory/

Pointers and the builtin container/view types built on them.

| Example | Shows |
|---------|-------|
| [pointers.mc](memory/pointers.mc) | `import`, heap allocation, `&` `*` `[]`, `sizeof`, `as` casts |
| [slices.mc](memory/slices.mc) | the builtin `slice<T>` view: borrowing a `list<T>` or `T[N]` with `as`, `.length`, indexing, `for x in`, passing by value, writing through |
| [slice_literals.mc](memory/slice_literals.mc) | array literals borrowed straight into a `slice<T>` over a hidden backing array: the explicit `as`, implicit adaptation at an annotated `let`, in slice-typed elements (nesting recurses), and at a bare function argument (through an overload set and a ternary too), the exact no-NUL length vs a named `char[N]`, the `{null, 0}` empty literal, writes through a mutable target, and the `@static slice<const T>` rodata view |
| [slice_assignment.mc](memory/slice_assignment.mc) | a string literal reborrowing into an existing char-slice lvalue by assignment (`s = "hi";`, no `as`, NUL dropped): all five lvalue forms — plain name, member (the `c.name = "hi"` gap-closer mirroring the struct literal), deref, index, and reference return — plus a `@static` char-slice global reassigned at runtime and a ternary of literals adapting arm by arm; array-literal assignment stays rejected (frame-local backing would dangle) |
| [sub_slices.mc](memory/sub_slices.mc) | `s[start:end]` narrowing a slice into a new view over the same storage: all four bound forms, writes landing in the shared storage, the verbatim `slice<const T>` result, chaining and direct iteration, index parity and unchecked bounds, the defined `s[n:n]` empty view, the destructuring sugar `let first, rest... = s` whose rest binder views the same storage (as unchecked as `s[i]`), the borrow-first rule for non-slice receivers, and the greedy ternary start |
| [lists.mc](memory/lists.mc) | `list<T>`, a growable random-access sequence: `push`, `get` (reference out-param), `from_array`, `append`, `duplicate` |
| [stacks.mc](memory/stacks.mc) | `stack<T>`, a growable LIFO: push and pop at the top |
| [queues.mc](memory/queues.mc) | `queue<T>`, a linked-list FIFO: push at the back, pop from the front, walk it with `for … in` |
| [rings.mc](memory/rings.mc) | `ring<T>`, an array-backed FIFO ring buffer: slot reuse as the indices wrap, `.at` logical indexing, doubling that re-lays wrapped elements in order |
| [intrusive_list.mc](memory/intrusive_list.mc) | the intrusive-container shape, `extends T` with a bare type parameter as the base: the payload embedded as the entry's layout prefix, its fields reached directly on the entry, the explicit `as` upcast handing the payload to list-unaware code |
| [nonnull_heap_buffers.mc](memory/nonnull_heap_buffers.mc) | a heap buffer crossing the stdlib's @nonnull contracts (memory copy/fill family, hashing digests): one diverging null guard after `alloc` covers every later call, the loops that leave the buffer alone included, and the same buffer behind a struct field surviving the write-free crc32 |

## systems/

Interfacing with the outside world: libc, the graded `@nonnull` promise on a
foreign declaration, formatted I/O (raw printf and the stdlib's native
`format` protocol), the std/char classification methods over libc's ctype,
shipping compiled libraries, and inline assembly.

| Example | Shows |
|---------|-------|
| [extern.mc](systems/extern.mc) | `@extern` functions (including variadic `...`), interfacing with libc |
| [extern_nonnull.mc](systems/extern_nonnull.mc) | `@nonnull` on an `@extern` declaration, graded by three postures over the opt-in `-Wextern-nonnull` class: a possibly-null argument silently accepted (relaxed default), warned (`-Wextern-nonnull` / `-Wall`), or a hard error with the LLVM hint restored (`-Werror=extern-nonnull`, or global `-Werror`); the always-rejected `null` literal and native-`@nonnull` contrasts, and the selective `-Werror=<class>` input form |
| [c_struct_abi.mc](systems/c_struct_abi.mc) | passing and returning a `struct` BY VALUE across the `@extern` C boundary, classified for the platform ABI (Apple/AAPCS64): libc's `div`/`ldiv` returning small structs in a register and a register pair, the homogeneous-float / ≤16-byte-GPR / >16-byte-indirect rules, and the compile error on non-AArch64 targets. Cross-compiled to an AArch64 object in CI; the full shape matrix round-trips against a linked C fixture in the test suite |
| [byte_scan.mc](systems/byte_scan.mc) | pointer arithmetic, C's element-scaled semantics under the shipped operators: `p + n` / `p - n` (exactly `&p[n]`) and the compound `p += n`, `p - q` as the signed element distance, the `while (p < end)` ordering scan-loop idiom, and `uint8*` byte arithmetic in a memchr-style byte scanner |
| [io.mc](systems/io.mc) | printf format specifiers, `puts`, `putchar`, string escapes |
| [formatting.mc](systems/formatting.mc) | the stdlib `format` overload set: direct calls appending each value's rendering into a `string`, the modifier grammars (integer `[0][width][x|X|b|p]`, float `[[N].M]f`, string `[N][s][N]`, bool `"yes"`), per-element slice rendering (nesting recurses), the `<typename>` fallback, one user overload making a struct printable, then the consumer split: `print`/`println` writing one string verbatim while `.format` carries the `{}` placeholders and their positional `{n}` sugar (duplicating, reordering, `{0:x}` colon modifiers, the `{:N}` width escape), and f-string interpolation (`{expr}` holes, `{expr:modifier}`, the `{n=}` inspector) rendering to owned string values ([types/fstring_values.mc](types/fstring_values.mc)) that `println` binds directly |
| [char_methods.mc](systems/char_methods.mc) | the std/char module, methods on the builtin `char` wrapping libc's ctype: the seven `char::is_*` classification predicates, `char::upper` / `char::lower` (non-letters unchanged), case-insensitive comparison and hex-token validation built from them, a classifying scan over a borrowed char slice, and in-place shouting through slice writes; the LIBRARY side of the builtin-qualifier feature [types/method_alias.mc](types/method_alias.mc) demonstrates |
| [interfaces.mc](systems/interfaces.mc) | bodyless `fn` prototypes (the mcc-convention counterpart to `@extern`) and the `--emit-interface` / `.mci` flow for shipping a compiled library: `&`/`const &`-struct exports, what ships as a prototype vs in full, the compile-then-link consumer side |
| [inline_asm.mc](systems/inline_asm.mc) | `@asm fn` and the `@asm(...)` expression, `$out`/`$N` operands and `:w` register modifiers, gated by `@if` on `TARGET_ARCH` |

## programs/

Complete little programs that put the pieces together.

| Example | Shows |
|---------|-------|
| [fizzbuzz.mc](programs/fizzbuzz.mc) | the classic, with `%` and an `else if` chain |
| [primes.mc](programs/primes.mc) | trial division: bool-returning helper, nested loops |

## baremetal/

The exception to "runnable with `--run`": [baremetal/](baremetal/) is a
freestanding qemu kernel cross-compiled with `--target`, with `@volatile`
MMIO and its own build instructions.
